<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Statement</title>
  </head>

  <body>
    <% if(fruit == 'apple' ) { %>
    <h1>Your fruit color is Red</h1>
    <% } else if (fruit == 'banana') { %>
    <h1>Your fruit color is Orange</h1>
    <% } else { %>
    <h1>I don't know your fruit color</h1>
    <% } %>
  </body>
</html>
