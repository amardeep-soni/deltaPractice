let arr = [5, 6, 17, 8, 10, 15];
let num = 8;

function returnLargerNumber(arr, num) {
    for (let i = 0; i < numbers.length; i++) {
        const element = numbers[i];
        if (element > num) {
            console.log(element);
        }
    }
}

returnLargerNumber(numbers, 8);